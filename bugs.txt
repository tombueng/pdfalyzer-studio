Bugs - FIXED (2026-03-01):
✓ veraPDF now runs using embedded Maven dependencies (no external PATH CLI requirement).
✓ Closing an image node now removes the node info block correctly.
✓ Form fields can be selected by clicking in PDF view; toolbar options activation and Ctrl/Shift multi-select work.
✓ Applying form-field options no longer reloads the PDF view or jumps back to page 1.

Open Bugs:
none
- attachment preview (pdf/img) should not open the "save as popup" but display in browser tab directly withou downloading. make it display inline in new tab.
- height and icon size of toolbar buttons still inconsistent, the form-add-field-buttons are to small.
- Form fields can still not be made "selected" by clicking in PDF view; toolbar options activation and Ctrl/Shift multi-select work.



