add support for passowrd protected / encrypted documents. when the user upload such a document we shall detect what we need. owner or user password. let the user give us the passwrod, we will figure out which type it is (by trying out). display in the UI bottom bar the details about the encryption. if we can not modify the file (we will try to figure out anyway and ignore write protection if feasible) we will display that in the UI and disallow modification operations.

